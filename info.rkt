#lang info
(define collection "while-until")
(define scribblings '(("while-until.scrbl")))
(define deps '("base"
               "compatibility-lib"
               "pprint-all"
               "while-loop"
               ))
(define build-deps '("racket-doc"
                     "scribble-lib"))
